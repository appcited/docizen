<article data-pagefind-body>
  <slot />
</article>

<style>
article {
  width: 100%;
  padding: 1.5rem;

  display: flex;
  flex-direction: column;
  row-gap: var(--theme-content-gap);
  max-width: var(--theme-content-max-width);
  margin: 0 auto;

  h1, h2, h3, h4, h5, h6 {
    color: var(--theme-heading-color);
    font-weight: var(--theme-text-weight-bold);
    margin-top: 0.5rem;
  }

  h1 {
    font-size: var(--theme-heading-size-1);
    margin: 1rem 0;
  }

  h2 {
    font-size: var(--theme-heading-size-2);
  }

  h3 {
    font-size: var(--theme-heading-size-3);
  }

  h4 {
    font-size: var(--theme-heading-size-4);
  }

  h5 {
    font-size: var(--theme-heading-size-5);
  }

  h6 {
    font-size: var(--theme-heading-size-6);
  }

  ul {
    list-style-type: disc;
    padding-left: calc(18px + var(--theme-content-indent-size));
  }

  ol {
    list-style-type: decimal;
    padding-left: calc(18px + var(--theme-content-indent-size));
  }

  ul, ol {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  a {
    text-decoration: underline;

    &:hover {
      color: var(--theme-text-color-highlight);
    }
  }

  img {
    border-radius: var(--theme-border-radius);
  }

  code {
    background-color: var(--theme-code-color-background);
    border-radius: var(--theme-code-border-radius);
    padding: 0 4px;
  }

  line-height: 28px;
}
</style>
